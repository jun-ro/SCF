local Framework = {
	-- Util --

	["SNet"] = require(script.Util.SNet),
	["Animation"] = require(script.Util.Animation),
	["Util"] = require(script.Util.Util),

	-- Combat --

	["StateManager"] = require(script.Combat.StateManager),
}

return Framework
